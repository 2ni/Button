Simple library to distinguish between long and short pushes
on a button for ESP32.

Button must be debounced (by hardware).

This library does not use any timers.

The use of a LED for feedback is optional.

# Example usage
see examples/

